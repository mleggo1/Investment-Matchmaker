{
  "name": "mlg-react-githubpages-template",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "homepage": "https://mleggo1.github.io/REPLACE_ME/",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "publish": "git add . && git commit -m \"update\" && git push && npm run deploy"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.0",
    "vite": "^5.0.0",
    "gh-pages": "^6.0.0"
  }
}
